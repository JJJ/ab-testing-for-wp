name: Checks

on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Use Node.js
      uses: actions/setup-node@v1
    - name: Install
      run: npm ci
    - name: Lint
      run: npm run lint
    - name: Flow Type
      run: npm run flow
    - name: Build
        npm run build
      env:
        CI: true
