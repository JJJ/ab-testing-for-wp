describe('Stand alone A/B tests', () => {
  it('Should allow the user to create a stand alone test', () => {
    expect(1).to.equal(1);
  });

  it('Enable to pick stand alone tests when adding A/B test in content', () => {
    expect(1).to.equal(1);
  });
});
